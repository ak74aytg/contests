Recursive Algorithm

f(idx){
    if(idx==0) return a[idx];
    if(idx<0) return 0;

    pick = a[idx] + f(idx-2);
    notPick = 0 + f(idx-1);

    return max(pick, notPick);
}




Tabulation Method

f(a)
{
    dp[0] = a[0]
    int neg = 0;

    for(int i=1;i<n;i++)
        pick = a[i]; if i>1 take+= dp[i-2];
        notPick = 0+dp[i-1]
        dp[i] = max(pick, notPick)

}





